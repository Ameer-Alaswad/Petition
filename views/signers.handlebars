<div class="div-container" id="signers-copntainer">
    <h1 class="title">All signers</h1>
    <div class="container">
        {{#allSigners}}
        {{#if url}}
        <h2><a href="{{url}}">{{first_name}} {{last_name}}</a> {{age}}</h2>
        {{/if}}
        <h2>{{first_name}} {{last_name}} {{age}} <a href="/signers/{{city}}">{{city}}</a></h2>
        {{/allSigners}}
    </div>

</div>